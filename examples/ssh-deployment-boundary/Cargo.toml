[package]
name = "artifact-ssh-deployment-boundary"
version = "0.1.0"
edition = "2024"

[dependencies]
artifact = { path = "../.." }
serde_json = "1.0.151"
tempfile = "3.27.0"
zip = { version = "2.4.2", default-features = false }
