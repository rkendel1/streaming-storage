[package]
name = "artifact-workbench"
version = "0.1.0"
edition = "2024"

[dependencies]
artifact = { path = "../.." }
artifact-runtime-consumer = { path = "../runtime-consumer" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
tempfile = "3.27.0"
